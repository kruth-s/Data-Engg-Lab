name: 'sales_project'
version: '1.0'
config-version: 2

profile: 'sales_project'

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_modules"]

models:
  sales_project:
    +materialized: table
